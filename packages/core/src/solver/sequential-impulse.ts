import type { ContactManifold, MatchaBuffers, WorldConfig } from '@matcha2d/types'

/**
 * Sequential impulse constraint solver.
 * TODO: Implement velocity and position solving.
 */
export function solveVelocity(
  _buffers: MatchaBuffers,
  _manifolds: ContactManifold[],
  _config: WorldConfig,
): void {
  // stub
}

export function solvePosition(
  _buffers: MatchaBuffers,
  _manifolds: ContactManifold[],
  _config: WorldConfig,
): void {
  // stub
}
